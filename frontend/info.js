//Propriedades: usamos as props para enviar uma informação junto a um determinado componente, uma vez que os componentes podem ter diferentes informaçoes em determinadas paginas.

//Estado: Estado, sao estruturas capazes de renderizar as informaçoes da tela sempre que sofrerem algum tipo de alteraçao.
//Nao podem ser modificados diretamente, apenas com a funcao de retorno do useState.

//import React, { useState } from 'react';
/* import Header from './Header';

function App() {
  let [counter, setCounter] = useState(0);
  //return Array [valor, funcaoAtualizacao]
  function increment(){
    setCounter(counter+1);    
  }
  return (
    <div>
      <Header>Contador: {counter}</Header>
      <button onClick={increment}>incrementar</button>
    </div>
  );
}

export default App; */